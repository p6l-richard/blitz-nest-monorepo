{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "api": "pnpm --filter server",
    "web": "pnpm --filter web"
  },
  "devDependencies": {
    "@nestjs/cli": "^9.1.3",
    "eslint": "7.32.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  }
}
